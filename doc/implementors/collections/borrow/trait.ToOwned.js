(function() {var implementors = {};
implementors["strcursor"] = ["impl <a class="trait" href="https://doc.rust-lang.org/nightly/collections/borrow/trait.ToOwned.html" title="trait collections::borrow::ToOwned">ToOwned</a> for <a class="struct" href="strcursor/grapheme/struct.Gc.html" title="struct strcursor::grapheme::Gc">Gc</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
